{
  "name": "reverse-proxy",
  "version": "1.0.0",
  "description": "Reverse proxy para acessar sistema interno via Render",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6"
  },
  "engines": {
    "node": ">=18"
  }
}
